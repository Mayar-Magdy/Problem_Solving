/*#include <bits/stdc++.h>
using namespace std;
#define rep(n) for (int i = 0; i < n; ++i)
using ll = long long;
int main()
{
    int n , k;
    cin>>n>>k;
    int a[n] , sum=0;
    rep(n) cin>>a[i];
    sort(a,a+n);
    rep(k) sum+=a[i];
    cout<<sum;
    return 0;
}
*/
