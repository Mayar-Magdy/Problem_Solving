/*#include <bits/stdc++.h>
using namespace std;
#define rep(n) for (int i = 0; i < n; ++i)
using ll = long long;
int main()
{ int n, d, cnt=0;
pair<double,double>p;
cin>>n>>d;
rep(n){
    cin>>p.first>>p.second;
    if(sqrt((p.first*p.first)+(p.second*p.second)) <= d) cnt++;
}
cout<<cnt;

    return 0;
    }
*/
